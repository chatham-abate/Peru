package com.github.chathamabate.peru.preset.base;

/**
 * Out of the box example Base types.
 * NOTE, this whole package is in no way needed to use the operation set
 * functionalities. This is simply a multipurpose example type system.
 */
public enum BaseType {
    BYTE,
    SHORT,
    INT,
    LONG,
    FLOAT,
    DOUBLE,
    CHARACTER,
    ENUM,
    BOOLEAN,
    STRING,
    MAP,
    SEQUENCE,
    FUNCTION
}
