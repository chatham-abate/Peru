package org.perudevteam.type.base;

/**
 * Out of the box example Base types.
 * NOTE, this whole package is in no way needed to use the operation set
 * functionalities. This is simply a multipurpose type system.
 */
public enum BaseType {
    INT,
    LONG,
    FLOAT,
    DOUBLE,
    ENUM,
    BOOLEAN,
    CHARACTER,
    STRING,
    MAP,
    SEQ
}
